# Drive & Conda Environment Switcher 🔄

A simple Python utility to automate switching between drives, navigating folders, and activating Conda environments in Windows.  
Instead of manually typing commands, this script provides an **interactive menu** that guides you step by step.

---

## ✨ Features
- Choose which drive (C/D/E) you want to open.
- Automatically list all folders in the selected drive.
- Navigate into any folder you choose.
- Display all available Conda environments.
- Activate the selected Conda environment in a new terminal window.

---

## 📦 Requirements
- Python 3.x installed
- Conda installed and initialized for your shell (`conda init cmd.exe`)
- Windows OS (tested on Command Prompt / PowerShell)

---